import React from 'react';
import Notes from './Notes';

const Home = (props) => {
// console.log(props);
  return (
    <div>
      
      <Notes message={"Update"}/>
    </div>
  );
}

export default Home;
